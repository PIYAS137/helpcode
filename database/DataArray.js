export const DataArr = [
    {
        id: 1,
        parentTitle: "Firebase Auth",
        mainTitle: "Google Provider",
        importItem:`import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter';
import { okaidia } from 'react-syntax-highlighter/dist/cjs/styles/prism';`,
        mainCode: `const handleClickCode = () => {
            setStatus(!status)
            setTimeout(() => {
              setStatus(false)
            }, 2000)
          }`,
        "openCommand": "npm run dev"
    },
    {
        id: 2,
        parentTitle: "Firebase Auth",
        mainTitle: "Github Provider",
        importItem:`import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter';
import { okaidia } from 'react-syntax-highlighter/dist/cjs/styles/prism';`,
        mainCode: `const handleClickCode = () => {
            setStatus(!status)
            setTimeout(() => {
              setStatus(false)
            }, 2000)
          }`,
        "openCommand": "npm run dev"
    }
]